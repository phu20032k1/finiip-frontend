name: V113 Frontend Smoke

on:
  pull_request:
  push:
    branches:
      - agent/accounting-api-ui-v113

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check core app JavaScript
        run: node --check app.js
      - name: Check Accounting Suite JavaScript
        run: node --check accounting-suite.js
      - name: Check V112 operations JavaScript
        run: node --check accounting-operations-v112.js
      - name: Check V113 API feature router
        run: node --check accounting-feature-router-v113.js
      - name: Check V113 config loader
        run: node --check config.js
